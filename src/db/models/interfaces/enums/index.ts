export enum LeadStatus {
  NEW = "new",
  CANCELED = "canceled",
  CONFIRMED = "confirmed",
}
export enum ClientType {
  INDIVIDUAL = "individual",
  COMPANY = "company",
}
export enum SessionStatus {
  PENDING = "pending",
  CONFIRMED = "confirmed",
  SHOOTING = "shooting",
  EDITING = "editing",
  FINISHED = "finished",
  CANCELED = "canceled",
}
